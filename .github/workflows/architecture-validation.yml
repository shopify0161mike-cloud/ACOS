name: Enterprise Architecture Validation

on:
  push:
    paths:
      - 'config/capabilities/**'
      - 'config/domains/**'
      - 'config/events/**'
      - 'config/execution/**'
      - 'config/governance/**'
      - 'config/platform/**'
      - 'config/intelligence/**'
      - 'schemas/**'
      - 'docs/architecture/**'
      - 'scripts/**'
      - 'tests/**'
      - '.github/workflows/architecture-validation.yml'
  pull_request:
    paths:
      - 'config/capabilities/**'
      - 'config/domains/**'
      - 'config/events/**'
      - 'config/execution/**'
      - 'config/governance/**'
      - 'config/platform/**'
      - 'config/intelligence/**'
      - 'schemas/**'
      - 'docs/architecture/**'
      - 'scripts/**'
      - 'tests/**'
      - '.github/workflows/architecture-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: architecture-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  capability-architecture:
    name: Capability architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate capability registry
        run: python scripts/validate_capability_registry.py
      - name: Validate capability dependency graph
        run: python scripts/validate_capability_dependencies.py
      - name: Validate canonical capability diagrams
        run: python scripts/validate_capability_diagrams.py

  domain-architecture:
    name: Domain architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate domain registry
        run: python scripts/validate_domain_registry.py
      - name: Validate domain references and ownership
        run: python scripts/validate_domain_references.py
      - name: Reject circular domain dependencies
        run: python scripts/validate_domain_dependencies.py
      - name: Validate canonical event contracts
        run: python scripts/validate_event_registry.py

  execution-architecture:
    name: Execution architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate execution state, retry, failure and compensation controls
        run: python scripts/validate_execution_registry.py

  governance-architecture:
    name: Governance architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate policy, authority, risk, budget and approval controls
        run: python scripts/validate_governance_registry.py

  platform-architecture:
    name: Platform architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate platform services, resilience and security controls
        run: python scripts/validate_platform_registry.py

  intelligence-architecture:
    name: Intelligence architecture controls
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'jsonschema[format]>=4.23,<5' 'PyYAML>=6.0,<7'
      - name: Validate agents, models, prompts, memory, knowledge and evaluation controls
        run: python scripts/validate_intelligence_registry.py

  repository-compliance:
    name: Existing repository compliance suite
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install YAML dependency
        run: python -m pip install 'PyYAML>=6.0,<7'
      - name: Validate constitution
        run: python scripts/validate_constitution.py
      - name: Run compliance tests
        run: python -m unittest discover -s tests -p 'test_*.py'
